<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Gestion Stock{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        {# 1. LE STYLE (CSS) : On charge Bootstrap depuis Internet #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        
        {# 2. TES STYLES PERSO (Si tu en as besoin plus tard) #}
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        {# --- NAVBAR (Menu du haut) --- #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 shadow">
            <div class="container-fluid">
                <a class="navbar-brand fw-bold" href="#">📦 Gestion Stock</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_produit_index') }}">Produits</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_categorie_index') }}">Catégories</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_stock_index') }}">Gestion Stock</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_historique') }}">Historique</a>
                        </li>
                        
                        <li class="nav-item">
                            <span class="nav-link disabled">Stocks (Bientôt)</span>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        {# --- CORPS DE LA PAGE --- #}
        <div class="container">
            {% block body %}{% endblock %}
        </div>

        {# 3. LE MOTEUR (JS) : Pour que les menus déroulants fonctionnent #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        
        {% block javascripts %}
        {% endblock %}
    </body>
</html>