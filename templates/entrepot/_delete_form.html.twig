<form method="post" action="{{ path('app_entrepot_delete', {'id': entrepot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrepot.id) }}">
    <button class="btn">Delete</button>
</form>
